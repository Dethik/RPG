// import

describe('SMG', () => {
  test('should check predetermined stat values to SMG', () => {
    expect(SMG).toEqual({
      dex: 1,
      atk: 4,
      dmg: 5
    })
  })
});

// describe('AR')

// describe('SG')

// describe('Melee')

// (SMG, AR, SG, Melee, Launchers) Weapon Types